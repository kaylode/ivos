global:
  debug: True
  cfg_transform: configs/sample/transform.yaml
  save_dir: runs
  device: cuda:0
  weights: null
data: 
  dataset:
    name: Brats21Testset
    args:
      volume_dir: /content/brats21-processed/imagesTr
      label_dir: /content/brats21-processed/labelsTr
      target_shape: 240
  dataloader:
    name: DataLoader
    args:
      batch_size: 64
      drop_last: True
      shuffle: True
